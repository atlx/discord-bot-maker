import React, { Component } from "react";

export default class CommandEditor extends Component {
    render() {
        return (
            <div className="command-editor">
                hello world command editor
            </div>
        );
    }
}
